---
const { imgSrc, imgAlt, title, category } = Astro.props.skill
---

<div class='swiper-slide'>
  <div
    class='skill w-[180px] h-[180px] flex flex-col items-center justify-center gap-4 border-2 border-primary rounded-full m-auto'>
    <img class='w-[70px] h-[70px]' src={imgSrc} alt={imgAlt} />
    <div class='flex flex-col items-center'>
      <span class='font-semibold text-xl'>{title}</span>
      <small class='text-sm text-gray-400'>{category}</small>
    </div>
  </div>
</div>
